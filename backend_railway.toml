[build]
builder = "DOCKERFILE"
dockerfilePath = "./backend.railway.dockerfile"
watchPatterns = [
    "backend/requirements.txt",
    "backend/**",
    "backend_railway.toml",
    "backend.railway.dockerfile",
]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
